---
import ResourceCard from "./ResourceCard.astro";

interface Props {
  title: string;
  url: string;
}

const { title, url } = Astro.props;
---

<ResourceCard title={title} url={url} type="book" />
